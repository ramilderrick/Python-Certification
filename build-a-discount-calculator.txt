** start of main.py **


def apply_discount(price, discount):
    final_price = 0
    if isinstance(price, (int, float)): #checks to see if price is int or float data type
        pass
    else:
        failedstring1 = 'The price should be a number'
        return failedstring1

    if isinstance(discount, (int, float)): #checks to see if discount is int or float data type
        pass
    else:
        failedstring2 = 'The discount should be a number'
        return failedstring2

    if price <= 0: #checks if price is a valid number
        falseprice = 'The price should be greater than 0'
        return falseprice

    if discount < 0 or discount >100: #checks to see if discount is valid number
        falsediscount = 'The discount should be between 0 and 100'
        return falsediscount
    
    discount = discount / 100

    discounted_price = price * discount

    final_price = price - discounted_price
    
    return final_price

** end of main.py **

